---
description: Run TRICYS test suite and lint checks
agent: tricys-test
---

Run the TRICYS project quality checks:

1. Run `make format` to auto-format code
2. Run `make lint` to check code style
3. Run `make test` to execute the test suite

Report any failures with file paths and error details.
